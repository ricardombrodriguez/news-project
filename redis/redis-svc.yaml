apiVersion: v1
kind: Service
metadata:
  name: redis-cluster
  namespace: gic-group-6
  labels:
    app: redis
spec:
  clusterIP: None
  ports:
    - name: redis
      port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: redis